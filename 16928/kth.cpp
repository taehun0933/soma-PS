#include <iostream>
using namespace std;

int numOfLadders, numOfSnakes;
int board[101];

int main(void)
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    cin >> numOfLadders >> numOfSnakes;
    while (numOfLadders--)
    {
    }

    while (numOfSnakes--)
    {
    }
}